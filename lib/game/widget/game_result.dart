import 'package:flutter/material.dart';

class GameResult extends StatelessWidget {
  const GameResult({required this.isEnd, super.key});
  final bool isEnd;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
